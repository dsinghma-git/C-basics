/* Write a Program to calculate and display the volume of a
   box having its height (h=10cm),width (w=12cm) and
   depth (8cm).
*/
#include<stdio.h>

int main()
{
	int h =10;
	int w = 12;
	int d = 8;
	int v = h * w * d;
	printf("Volume of the box = %d",v);
	return 0;
}  
